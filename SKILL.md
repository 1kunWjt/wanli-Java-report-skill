---
name: java-report
description: 按《面向对象程序设计》Word 实验报告模板，收集学生信息与 Java 实验要求，生成含实验目的/内容/步骤/结果（题目+源代码+运行截图）/总结的 .docx 实验报告，并记住学生档案供下次复用。Use when 用户提到「写实验报告」「Java 实验报告」「面向对象程序设计 实验报告」「上交实验报告」「实验报告模板」「lab report」「java-report」「/java-report」，或要求按老师格式提交 Java 实践任务报告。不要用于普通 Java 代码讲解、非实验报告的 Word 写作、或非课程实验报告。
---

# Java 实验报告生成器

按浙江万里学院《面向对象程序设计》实验报告格式，把老师的实践任务整理成可上交的 Word 实验报告。

## 重要规则（不可跳过）

1. **先读记忆**，再向用户要信息；已有字段不要重复问。
2. **必须拿到「实验要求」全文**（含实验名称/实验目的/实验内容）才能写报告；用户没给就先要。
3. 「三、实验步骤」必须覆盖**所有题目**，用**像人类做题时的想法**来写，不要写成说明书腔。
4. 「四、实验结果」固定结构：每题 **题目 + 1）源代码 + 2）运行结果（截图）**。
5. **截图**：有真实截图就用；没有就按用户 Windows Terminal 真图样式自动生成（扁灰标签栏 + 纯黑字格正文），并告知可换成真实截图。
6. 写完后**保存记忆**，方便下次只改实验内容就能出报告。
7. 最终交付 **.docx**（不要输出 .doc）。生成后用 `present_files` 交给用户。

## 工作流

### Step 1：加载记忆

1. 读取本 skill 目录下 `memory/profile.json`（不存在则跳过）。
2. 同时用 `memory` 工具搜索：`java-report 学生档案`、`实验报告 课程 姓名 学号`。
3. 把命中的字段记入工作区变量：`school_name`、`course_name`、`student_name`、`student_id`、`class_name`、`lab_date` 默认值、`lab_name` 等。
4. 若记忆中有完整档案，用一句话展示并请用户确认是否沿用；用户确认沿用则跳到 Step 3。

### Step 2：收集报告元信息

需要的字段（缺什么问什么，一次问完）：

| 字段 | 说明 | 示例 |
|------|------|------|
| school_name | 学校名称（写入大标题） | 浙江万里学院 |
| course_name | 课程名称 | 面向对象程序设计 |
| lab_name | 实验名称 | 实验二 简单Java程序设计 |
| class_name | 专业班级 | 通信101 |
| student_name | 姓名 | 谭雯雁 |
| student_id | 学号 | 2010014177 |
| lab_date | 实验日期 | 2025.11.09 或 11.09.08 |

说明：

- 记忆里已有的默认值直接展示给用户确认：「已记住：姓名 xxx / 学号 xxx / …，本次是否沿用？」
- 用户说「沿用/一样/不用改」就跳过。
- 封面字段排版按模板：`专业班级：xxx姓名：xxx学号：xxx实验日期：xxx`。

### Step 3：收集实验要求

要求用户发送老师给的实验要求全文，至少包含：

- 实验名称
- 实验目的（多条）
- 实验内容（多道题目）

参考格式：

```text
实验名称：数据类型与运算操作符号
实验目的：
    1.熟悉java的基本数据类型与操作符号
    2.熟悉java程序设计的基本方法
实验内容：
1、定义一个整型的长度为6的一维数组k[6]，并将数组中元素k[i] 值初始化为i。然后，将元素k[3]打印出来。
2、定义一个整型的长度为3 x 4的二维数组k[3][4]……
3、从命令行输入几个字符串，统计并打印出输入字符串的个数、以及各字符串的字符个数。
4、从命令行输入一个数字，判断它是奇数还是偶数。
5、从命令行输入两个数字，判断两个数谁大谁小。
```

解析出：

- `purpose`：实验目的（保留 1. 2. 3. 条目）
- `content_summary`：实验内容概述（可按题目列表摘要）
- `problems[]`：每道题的题干原文

若用户同时贴了已完成的代码/截图，一并收进对应题目。

### Step 4：撰写「三、实验步骤」（人类思路）

对 `problems[]` **每一题**写一段做题思路。必须像人边想边做，而不是 API 说明。

写法要求：

- 用「我/先/然后/如果/最后」这类自然口语逻辑链。
- 说清楚：怎么拿输入 → 怎么处理 → 怎么判断/计算 → 怎么输出。
- 提到关键 API 时自然带出（如 `args`、`Integer.parseInt`、`%`、数组下标），像想起来的，不像背定义。
- 每题 2～5 句，不要过长；多题时用 `实验1：` `实验2：` 分段。

正例：

> 实验4：命令行传进来的是字符串，得先用 Integer.parseInt 转成 int 才好判断。奇偶就看除以 2 的余数，用 % 最省事：余 0 是偶数，否则是奇数。输出时分别打印就行，if/else 或三元都可以，我用 if/else 更直观。

反例（禁止）：

> 本实验旨在掌握 Java 基本数据类型。首先定义变量，然后使用取模运算符进行判断……（空洞、像教学大纲）

### Step 5：设计源代码

为每题写可运行的简单 Java 代码，风格参照学生实验（见 `references/code-style.md`）：

- 一个完整类 + `public static void main(String[] args)`。
- 类名简单直接：`t1`/`t2`/`Test1`/`Hello`/`Basic` 等，或按题意命名。
- 代码保持课堂水平：数组、循环、`args`、`Integer.parseInt`、`System.out.println`、if/else、三元即可。
- 不要引入文件 IO、集合框架、第三方库。
- 命令行输入题照常用 `args` / `Integer.parseInt`，但**报告里不要写「运行示例：java …」**。

源代码放在「四、实验结果」里，「三、实验步骤」只写思路，不贴大段代码。

### Step 6：运行结果截图

优先顺序：

1. **用户提供了真实截图** → 填进 `problems[].screenshots`，用用户的图。
2. **用户没给截图** → 标签栏 **1:1 粘贴** `assets/tab-chrome.png`；正文对照 java 帮助真图：`提示符+命令` 开始（无系统横幅）→ 程序输出 → **末尾空提示符+块光标**；行距 23px、右侧滚动条。不要只留空占位：
   - 脚本会尝试**真实编译并运行** Java 代码（中文 UTF-8/GBK 自适应）。
   - 命令行输入题，在 JSON 里写 `run_hint`。
   - 生成的图会嵌进「2）运行结果（截图）」。
3. 生成后仍可一句话提醒：「已自动合成 Win11 终端运行截图；若要换成你本机真实截图，把图发我即可替换。」

单图也可手动生成：

```bash
"$MIMO_PYTHON" scripts/generate_console.py out.png --cmd "java t3 hello java world" --code T3.java
```

### Step 7：撰写「五、实验总结」

150～300 字，口吻像学生自己复盘：

- 这次实验做了什么（结合实验目的）。
- 踩过的坑（类型转换、args、数组下标、取模边界等，有多少写多少）。
- 学到了什么、下次注意什么。

不要写成论文摘要。

### Step 8：生成 Word

把数据写成 JSON，调用：

```bash
"$MIMO_PYTHON" scripts/generate_report.py <data.json> <output.docx>
```

脚本相对本 skill 目录。输出文件名建议：

`实验报告-{lab_name}-{student_name}.docx`

放在用户指定位置；未指定则放在当前工作目录。

生成后：

1. 用 `"$MIMO_PYTHON"` 打开 docx 确认无异常（脚本自带基本校验）。
2. `present_files` 交付 docx。
3. 进入 Step 9 保存记忆。

JSON 最小结构见 `references/report-format.md`。

### Step 9：保存记忆

1. 写入本 skill 目录 `memory/profile.json`：

```json
{
  "school_name": "",
  "course_name": "",
  "class_name": "",
  "student_name": "",
  "student_id": "",
  "lab_date": "",
  "lab_name": "",
  "updated_at": "ISO-8601"
}
```

2. 用 `memory` 工具写入一条便于检索的档案摘要（scope=global），便于其他会话找回。
3. 对用户说一句：「已记住你的档案，下次直接发实验要求即可。」

## 报告版式（必须保留模板开头方框/下划线）

```text
        浙江万里学院实验报告

                    ┌──────────────┐
课程名称：________  │ 成绩：______  │
      　　{课程名称}____ │ 教师：______  │
实验名称：{lab_name}____ └──────────────┘
专业班级：{班级}姓名：{姓名}学号：{学号}实验日期：{日期}

┌─────────────────────────────┐
│ 一、实验目的：                │
│ {purpose}                    │
│ 二、实验内容：                │
│ {content_summary 或题目列表}  │
│ 三、实验步骤：                │
│ 实验1：{人类思路}             │
│ 四、实验结果：                │
│ 实验1：{题干}                 │
│ 1）源代码                     │
│ {code}                       │
│ 2）运行结果（截图）            │
│ {截图或占位}                  │
│ 五、实验总结：                │
│ {summary}                    │
└─────────────────────────────┘
```

硬性版式（生成脚本 `scripts/generate_report.py` 已实现，勿改回纯文本）：

1. 右侧**成绩/教师方框**（细线矩形），与左侧课程/实验名称两行并排。
2. 课程名称、实验名称、班级行填入值带**下划线**（填表感）。
3. 从「一、实验目的」到「五、实验总结」整体套**正文大边框**。
4. 成绩/教师在开头方框内，**不要**放到文末。

细节、JSON 模板与示例：读 `references/report-format.md`。完整可运行样例数据：`assets/sample-data.json`。

## 示例

**用户**：帮我写实验报告，实验要求如下……（贴出数据类型与运算题）

**做法**：

1. 读记忆拿姓名学号；缺则问课程名/实验名/班级/日期。
2. 解析出 5 道题。
3. 每题写人类思路 + 简单 Java 代码。
4. 提醒提供 5 张运行截图。
5. 有图则嵌入，无图留占位。
6. 写实验总结，生成 docx，保存记忆。

## 故障排查

| 问题 | 处理 |
|------|------|
| 找不到 memory/profile.json | 当作首次使用，完整收集元信息 |
| 用户只给了实验名称没给内容 | 先要完整实验要求，不要瞎编题目 |
| python-docx 报错 | 用 `"$MIMO_PYTHON" -m docx` 路径调用；确认走的是托管 Python |
| 截图路径不存在 | 再要一次；占位继续出报告 |
| 生成的 docx 打开有修复提示 | 跑 `scripts/generate_report.py` 自检；必要时用 docx audit |
| 用户改学校/格式 | 只改对应字段或提示可调整；版式默认不要乱改 |
